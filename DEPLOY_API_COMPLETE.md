# ✅ API сервер задеплоен!

## 📡 URL API сервера:
**https://devid678-a11y-simplebot-cfb4.twc1.net**

## ⚙️ Настройка переменных окружения

**ВАЖНО:** Переменные окружения нужно настроить вручную в панели Timeweb:

1. Зайдите в https://console.timeweb.cloud
2. Найдите приложение **"dvizh-api-server"**
3. Перейдите: **Настройки** → **Переменные окружения**
4. Добавьте:

```env
DATABASE_URL=postgresql://gen_user:c%-5Yc01xe*Bdf@7cedb753215efecb1de53f8c.twc1.net:5432/default_db?sslmode=require
PORT=3000
```

5. Сохраните и перезапустите приложение

## 🔗 Обновление веб-приложения

После настройки переменных окружения и проверки работы API:

### Вариант 1: Через .env.production (рекомендуется)

Создайте файл `web/.env.production`:

```env
VITE_API_BASE=https://devid678-a11y-simplebot-cfb4.twc1.net
```

Затем пересоберите и задеплойте:

```bash
cd web
npm run build
firebase deploy --only hosting
```

### Вариант 2: Через Firebase Hosting конфигурацию

Добавьте переменную окружения при сборке:

```bash
cd web
VITE_API_BASE=https://devid678-a11y-simplebot-cfb4.twc1.net npm run build
firebase deploy --only hosting
```

## ✅ Проверка работы

1. **API Health**: https://devid678-a11y-simplebot-cfb4.twc1.net/health
   - Должно вернуть: `{"status":"ok","database":"connected"}`

2. **Список событий**: https://devid678-a11y-simplebot-cfb4.twc1.net/api/events
   - Должен вернуть массив событий из PostgreSQL

3. **Веб-приложение**: https://dvizh-eacfa.web.app/
   - Должны появиться новые события из PostgreSQL

## 🎉 Готово!

После выполнения всех шагов веб-приложение начнет показывать новые события из PostgreSQL (Timeweb) вместо старых из Firestore!
