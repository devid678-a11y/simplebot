# Деплой API сервера на Timeweb Cloud

## 📋 Подготовка

1. Убедитесь, что код API сервера находится в репозитории на GitHub
2. Структура должна быть:
   ```
   api/
     server.js
     package.json
   ```

## 🚀 Вариант 1: Через Timeweb Cloud Console

### Шаг 1: Создание приложения

1. Зайдите в [Timeweb Cloud Console](https://console.timeweb.cloud)
2. Перейдите в раздел "Приложения"
3. Нажмите "Создать приложение"
4. Выберите:
   - **Тип**: Backend
   - **VCS провайдер**: Ваш GitHub провайдер
   - **Репозиторий**: Выберите репозиторий с API сервером
   - **Ветка**: `main` (или другая)
   - **Пресет**: Backend app (ID: 1003 или выше)
   - **Фреймворк**: `express`
   - **Build команда**: (оставить пустым)
   - **Run команда**: `node api/server.js`

### Шаг 2: Настройка переменных окружения

В настройках приложения добавьте:

```
DATABASE_URL=postgresql://gen_user:c%-5Yc01xe*Bdf@7cedb753215efecb1de53f8c.twc1.net:5432/default_db?sslmode=require
```

Или отдельными параметрами:

```
DB_HOST=7cedb753215efecb1de53f8c.twc1.net
DB_PORT=5432
DB_NAME=default_db
DB_USER=gen_user
DB_PASSWORD=c%-5Yc01xe*Bdf
DB_SSL=true
PORT=3000
```

### Шаг 3: Деплой

1. Нажмите "Деплой"
2. Дождитесь завершения сборки
3. Скопируйте URL приложения (будет что-то вроде `https://your-app-id.twc1.net`)

## 🔗 Обновление веб-приложения

После деплоя API сервера обновите веб-приложение:

1. В `web/.env.local` добавьте:
   ```env
   VITE_API_BASE=https://your-api-app-id.twc1.net
   ```

2. Пересоберите и задеплойте веб-приложение:
   ```bash
   cd web
   npm run build
   firebase deploy --only hosting
   ```

## 📝 Проверка

После деплоя проверьте:

1. API health check: `https://your-api-app-id.twc1.net/health`
2. Список событий: `https://your-api-app-id.twc1.net/api/events`

Если всё работает, веб-приложение начнёт показывать новые события из PostgreSQL!

