# 🚀 Деплой API сервера на Timeweb Cloud - Пошаговая инструкция

## ✅ Что уже готово:
- ✅ API сервер создан (`api/server.js`, `api/package.json`)
- ✅ Репозиторий: `https://github.com/devid678-a11y/simplebot.git`
- ✅ VCS провайдер настроен в Timeweb

## 📝 Шаги для деплоя:

### 1. Закоммитьте и запушьте API код в репозиторий:

```bash
# Добавьте новые файлы
git add api/
git commit -m "Add API server for PostgreSQL"
git push origin main
```

### 2. Получите SHA последнего коммита:

```bash
git rev-parse HEAD
```
Скопируйте 40-символьный SHA (например: `abc123def456...`)

### 3. Создайте приложение через Timeweb Console:

1. Зайдите на https://console.timeweb.cloud
2. Перейдите: **Приложения** → **Создать приложение**
3. Заполните:
   - **Тип**: `backend`
   - **Название**: `dvizh-api-server`
   - **VCS провайдер**: Выберите ваш GitHub провайдер
   - **Репозиторий**: `https://github.com/devid678-a11y/simplebot.git`
   - **Ветка**: `main`
   - **SHA коммита**: Вставьте SHA из шага 2
   - **Пресет**: `1003` (Backend app, 188₽/мес) или `1631` (250₽/мес)
   - **Фреймворк**: `express`
   - **Build команда**: (оставить пустым)
   - **Run команда**: `node api/server.js`

### 4. Добавьте переменные окружения:

В настройках приложения → **Переменные окружения**, добавьте:

```env
DATABASE_URL=postgresql://gen_user:c%-5Yc01xe*Bdf@7cedb753215efecb1de53f8c.twc1.net:5432/default_db?sslmode=require
PORT=3000
```

### 5. Задеплойте:

Нажмите **Деплой** и дождитесь завершения.

### 6. Получите URL приложения:

После деплоя скопируйте URL приложения (например: `https://abc123.twc1.net`)

### 7. Обновите веб-приложение:

Добавьте в `web/.env.local`:

```env
VITE_API_BASE=https://your-api-url.twc1.net
```

Затем пересоберите и задеплойте:

```bash
cd web
npm run build
firebase deploy --only hosting
```

## ✅ Проверка:

1. Откройте: `https://your-api-url.twc1.net/health` - должно показать `{"status":"ok","database":"connected"}`
2. Откройте: `https://your-api-url.twc1.net/api/events` - должны показаться события
3. Проверьте сайт: `https://dvizh-eacfa.web.app/` - должны появиться новые события

## 🔄 Автоматизация через MCP (опционально):

Если хотите автоматизировать через MCP инструменты, скажите мне SHA коммита после пушей, и я создам приложение автоматически.

