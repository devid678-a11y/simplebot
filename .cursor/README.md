# Настройка MCP сервера для Timeweb в Cursor

## 📋 Что это дает:

- **Прямое управление** Timeweb Cloud из Cursor
- **Автоматический деплой** приложений
- **Управление контейнерами** и сервисами
- **Мониторинг** и логи

## 🔧 Настройка:

### 1. Получите API токен Timeweb:
- Зайдите в панель Timeweb Cloud
- Перейдите в "Настройки" → "API токены"
- Создайте новый токен
- Скопируйте токен

### 2. Обновите конфигурацию:
Замените `your-api-token` в файле `.cursor/mcp.json` на ваш реальный токен:

```json
{
  "mcpServers": {
    "timeweb-mcp-server": {
      "command": "npx",
      "args": ["timeweb-mcp-server"],
      "env": {
        "TIMEWEB_TOKEN": "ваш_реальный_токен_здесь"
      }
    }
  }
}
```

### 3. Перезапустите Cursor:
- Закройте Cursor
- Откройте заново
- MCP сервер должен подключиться автоматически

## 🚀 Использование:

После настройки вы сможете:
- Управлять приложениями через Cursor
- Деплоить код напрямую
- Просматривать логи
- Управлять переменными окружения

## 🔍 Проверка подключения:

В Cursor должен появиться индикатор MCP сервера в статус-баре.
