# 🗺️ Интеграция Google Maps - Краткое руководство

## ✅ Что уже сделано

1. **Добавлены зависимости** в `build.gradle`:
   - `play-services-maps`
   - `play-services-location` 
   - `maps-compose`

2. **Добавлены разрешения** в `AndroidManifest.xml`:
   - `ACCESS_FINE_LOCATION`
   - `ACCESS_COARSE_LOCATION`

3. **Созданы компоненты**:
   - `EventMapView.kt` - мини-карта в карточке события
   - `EventDetailScreen.kt` - детальный экран с картой
   - `GeocodingService.kt` - сервис геокодирования

4. **Интегрировано в UI**:
   - Карта показывается только для офлайн событий
   - Автоматическое геокодирование адресов
   - Fallback на внешние карты

## 🔧 Что нужно сделать

### 1. Получить API ключ Google Maps
```bash
# Перейдите в Google Cloud Console
# Включите Maps SDK for Android
# Создайте API ключ
# Ограничьте ключ для Android приложений
```

### 2. Заменить API ключ в манифесте
```xml
<!-- В AndroidManifest.xml -->
<meta-data
    android:name="com.google.android.geo.API_KEY"
    android:value="AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" />
```

### 3. Добавить SHA-1 fingerprint
```bash
# Для debug версии
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android

# Добавьте SHA-1 в Google Cloud Console
```

## 🚀 Готово к использованию!

После настройки API ключа:
- Карты будут автоматически отображаться для офлайн событий
- Геокодирование будет работать для популярных адресов
- Пользователи смогут открывать карты в полном размере
- Fallback на внешние карты при ошибках

## 📱 Тестирование

1. Создайте тестовое событие с адресом в Москве
2. Проверьте отображение мини-карты
3. Нажмите на карту для открытия в полном размере
4. Протестируйте с разными адресами

## 🎯 Следующие шаги

- Добавить навигацию к детальному экрану события
- Реализовать кэширование координат
- Добавить больше городов в геокодирование
- Оптимизировать производительность карт


