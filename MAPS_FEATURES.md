# Функции Google Maps в приложении

## 🗺️ Реализованные возможности

### 1. Мини-карта в карточке события
- Показывается только для офлайн событий
- Автоматическое геокодирование адреса
- Интерактивная карта с маркером события
- Нажатие открывает полноэкранную карту

### 2. Полноэкранная карта
- Диалог с увеличенной картой
- Полные элементы управления (зум, компас, моя локация)
- Маркер с названием и адресом события

### 3. Внешние карты
- Fallback на Google Maps или другие приложения
- Открытие по адресу через geo: URI
- Веб-версия карт как последний fallback

### 4. Геокодирование
- Поддержка Google Geocoding API
- Fallback на простое геокодирование для популярных мест
- Кэширование результатов

## 🚀 Как использовать

### В карточке события:
```kotlin
EventMapView(
    location = event.location!!,
    eventTitle = event.title,
    modifier = Modifier.fillMaxWidth()
)
```

### В детальном экране:
```kotlin
EventDetailScreen(
    event = event,
    onBack = { /* навигация назад */ }
)
```

## ⚙️ Настройка

1. **Получите API ключ** в Google Cloud Console
2. **Замените** `YOUR_GOOGLE_MAPS_API_KEY` в `AndroidManifest.xml`
3. **Ограничьте ключ** только для вашего приложения
4. **Добавьте SHA-1** fingerprint для безопасности

## 📱 Поддерживаемые адреса

### Москва:
- Парк Горького
- Циферблат
- Тверская улица
- Арбат
- Красная площадь
- Сокольники

### Другие города:
- Санкт-Петербург
- Казань
- Екатеринбург
- Новосибирск

## 🔧 Кастомизация

### Изменение стиля карты:
```kotlin
properties = MapProperties(
    mapType = MapType.SATELLITE, // или HYBRID, TERRAIN
    isMyLocationEnabled = true
)
```

### Настройка маркера:
```kotlin
Marker(
    state = MarkerState(position = coordinates),
    title = eventTitle,
    snippet = location,
    icon = BitmapDescriptorFactory.defaultMarker(BitmapDescriptorFactory.HUE_BLUE)
)
```

## 🐛 Отладка

### Проверка API ключа:
- Убедитесь, что ключ активен в Google Cloud Console
- Проверьте ограничения по приложению
- Убедитесь, что включены нужные API

### Проверка геокодирования:
- Логи в `GeocodingService.kt`
- Fallback на простое геокодирование
- Тестирование с известными адресами

## 📊 Производительность

- Ленивая загрузка карт
- Кэширование координат
- Оптимизированные запросы к API
- Graceful fallback при ошибках


