# Проверка статуса API сервера в Timeweb

## Проблема
API сервер `https://devid678-a11y-simplebot-a491.twc1.net` недоступен с ошибкой `ERR_SSL_PROTOCOL_ERROR`.

## Что проверить в панели Timeweb

1. **Зайдите в панель управления Timeweb:**
   - URL: https://console.timeweb.cloud
   - Войдите в аккаунт

2. **Найдите приложение:**
   - Перейдите в раздел **"Приложения"** или **"Apps"**
   - Найдите приложение **"dvizh-api-server-v4"**

3. **Проверьте статус:**
   - **Статус должен быть**: "Running" / "Запущено" / "Работает"
   - Если статус "Building" / "Сборка" - подождите 2-5 минут
   - Если статус "Stopped" / "Остановлено" - запустите приложение

4. **Проверьте логи:**
   - Нажмите на приложение
   - Откройте вкладку **"Логи"** или **"Logs"**
   - Должны быть сообщения:
     - `✅ PostgreSQL подключен`
     - `🚀 API сервер запущен на порту 3000`
   - Если есть ошибки - скопируйте их и отправьте

5. **Проверьте настройки:**
   - **Run команда**: должно быть `node api/server.js`
   - **Build команда**: `npm install` (или пусто)
   - **Порт**: `3000`
   - **URL**: должен быть `https://devid678-a11y-simplebot-a491.twc1.net`

6. **Проверьте SSL:**
   - В настройках приложения должна быть включена опция SSL/HTTPS
   - Timeweb обычно автоматически настраивает SSL через Let's Encrypt
   - Это может занять до 10 минут после первого запуска

## Если приложение не запускается

### Вариант 1: Проверьте путь к файлу
- Убедитесь, что в репозитории есть файл `api/server.js`
- Run команда должна быть: `node api/server.js` (не `node api-server.js`)

### Вариант 2: Проверьте зависимости
- В логах должна быть успешная установка: `npm install`
- Если есть ошибки установки - исправьте их

### Вариант 3: Проверьте подключение к БД
- В логах должна быть строка: `✅ PostgreSQL подключен`
- Если ошибка подключения - проверьте учетные данные (они встроены в код)

## После исправления

1. Подождите 1-2 минуты после запуска/перезапуска
2. Проверьте в браузере: `https://devid678-a11y-simplebot-a491.twc1.net/health`
   - Должно вернуть: `{"status":"ok","database":"connected"}`
3. Проверьте события: `https://devid678-a11y-simplebot-a491.twc1.net/api/events`
   - Должен вернуть массив событий из PostgreSQL
4. Обновите веб-приложение: `https://dvizh-eacfa.web.app/`
   - События должны появиться автоматически

## Альтернативное решение

Если Timeweb API не работает, можно использовать:
- **Railway** - автоматический HTTPS, простое развертывание
- **Render** - бесплатный тариф, автоматический HTTPS
- **Firebase Functions** - требует платный план (Blaze)

Проверьте статус приложения в Timeweb и пришлите информацию из логов, если есть ошибки.

