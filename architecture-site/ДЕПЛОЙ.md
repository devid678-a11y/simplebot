# 🚀 Инструкция по деплою сайта

## ✅ Проект готов к деплою!

Сборка успешна. Все файлы в папке `dist/`.

---

## 🌟 СПОСОБ 1: Vercel (Рекомендуется - самый простой)

### Через CLI:

1. **Авторизуйтесь:**
   ```bash
   cd architecture-site
   npx vercel login
   ```

2. **Задеплойте:**
   ```bash
   npx vercel --prod
   ```

3. Следуйте инструкциям в терминале

### Через веб-интерфейс (ЕЩЕ ПРОЩЕ):

1. Откройте: **https://vercel.com/new**
2. Войдите через **GitHub**
3. Выберите репозиторий `simplebot`
4. Укажите **Root Directory**: `architecture-site`
5. Нажмите **Deploy**
6. Готово! Получите URL вида: `https://your-site.vercel.app`

**Преимущества:**
- ✅ Бесплатно
- ✅ Автоматический HTTPS
- ✅ Автоматический деплой при каждом коммите
- ✅ Быстро и просто

---

## 🌐 СПОСОБ 2: Netlify (Очень просто)

### Через веб (САМЫЙ ПРОСТОЙ):

1. Откройте: **https://app.netlify.com/drop**
2. **Перетащите** папку `dist` в браузер
3. Готово! Получите URL

### Через CLI:

```bash
cd architecture-site
npx netlify-cli login
npx netlify-cli deploy --prod --dir=dist
```

---

## 📦 СПОСОБ 3: GitHub Pages

1. Установите gh-pages:
   ```bash
   npm install --save-dev gh-pages
   ```

2. Добавьте в `package.json`:
   ```json
   "scripts": {
     "predeploy": "npm run build",
     "deploy": "gh-pages -d dist"
   }
   ```

3. Задеплойте:
   ```bash
   npm run deploy
   ```

4. URL будет: `https://devid678-a11y.github.io/simplebot/architecture-site/`

---

## 🔧 СПОСОБ 4: Timeweb Cloud

Если хотите использовать Timeweb:

1. **Добавьте файлы в Git:**
   ```bash
   cd architecture-site
   git add .
   git commit -m "Add architecture site"
   git push
   ```

2. Затем используйте панель Timeweb Cloud для создания Frontend приложения:
   - Framework: React/Vite
   - Build команда: `npm install && npm run build`
   - Index directory: `/dist`

---

## 📋 Что уже настроено:

- ✅ `vercel.json` - конфигурация для Vercel
- ✅ `netlify.toml` - конфигурация для Netlify
- ✅ `public/_redirects` - редиректы для SPA
- ✅ `vite.config.js` - настроен base path
- ✅ Проект собран и готов

---

## 🎯 РЕКОМЕНДАЦИЯ

**Используйте Vercel через веб-интерфейс** - это самый быстрый способ:

1. https://vercel.com/new
2. Войти через GitHub
3. Выбрать репозиторий
4. Указать папку `architecture-site`
5. Deploy!

**Готово за 2 минуты!** ⚡

