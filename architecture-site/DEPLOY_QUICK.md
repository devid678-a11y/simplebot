# Быстрый деплой сайта

## ✅ Проект готов к деплою!

Сборка успешна. Файлы находятся в папке `dist/`.

## 🚀 Вариант 1: Vercel (Самый быстрый)

### Через CLI:
```bash
cd architecture-site
npx vercel --prod
```

Следуйте инструкциям:
- Выберите папку `architecture-site`
- Vercel автоматически определит настройки

### Через веб-интерфейс:
1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите репозиторий `simplebot`
5. Укажите Root Directory: `architecture-site`
6. Нажмите Deploy

## 🌐 Вариант 2: Netlify

### Через CLI:
```bash
cd architecture-site
npm install -g netlify-cli
netlify login
netlify deploy --prod --dir=dist
```

### Через веб-интерфейс:
1. Зайдите на https://netlify.com
2. Перетащите папку `dist` в браузер
3. Готово!

## 📦 Вариант 3: GitHub Pages

Если проект уже в Git:
```bash
cd architecture-site
npm install --save-dev gh-pages
npm run deploy
```

## 🔧 Настройки для деплоя

Все файлы конфигурации уже созданы:
- ✅ `vercel.json` - для Vercel
- ✅ `netlify.toml` - для Netlify  
- ✅ `public/_redirects` - для редиректов
- ✅ `vite.config.js` - настроен base path

## 📝 Важно

Перед деплоем убедитесь, что:
- ✅ Проект собран (`npm run build`)
- ✅ Все файлы закоммичены в Git (если используете Git деплой)
- ✅ Проверена работа локально (`npm run preview`)

## 🎯 Рекомендация

Используйте **Vercel** - это самый простой и быстрый способ:
- Бесплатно
- Автоматический HTTPS
- Автоматический деплой при пуше в Git
- Быстро

