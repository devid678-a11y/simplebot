# 🔧 Исправление ошибки 404 (Not Found)

## ❌ Проблема:

При переходе на `/blog` или `/contact` появляется ошибка:
```
Not Found
The requested URL was not found on this server.
```

## 🔍 Причина:

Это проблема с SPA (Single Page Application). Сервер не знает, что все запросы должны перенаправляться на `index.html`.

---

## ✅ Решение 1: Файл .htaccess (для Apache)

### Создайте файл `.htaccess` в корне сайта:

1. **В файловом менеджере Timeweb** перейдите в `public_html`

2. **Создайте файл** `.htaccess` (если его нет)

3. **Добавьте код:**
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

4. **Сохраните файл**

5. **Проверьте** - страница `/blog` должна открываться

---

## ✅ Решение 2: Файл web.config (для IIS)

Если Timeweb использует IIS (Windows сервер):

1. **Создайте файл** `web.config` в `public_html`

2. **Добавьте код:**
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <configuration>
     <system.webServer>
       <rewrite>
         <rules>
           <rule name="React Routes" stopProcessing="true">
             <match url=".*" />
             <conditions logicalGrouping="MatchAll">
               <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
               <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
             </conditions>
             <action type="Rewrite" url="/index.html" />
           </rule>
         </rules>
       </rewrite>
     </system.webServer>
   </configuration>
   ```

---

## ✅ Решение 3: Настройка в панели Timeweb

### Через панель управления:

1. **Панель Timeweb** → **Хостинг** → **Сайты**

2. **Выберите ваш сайт**

3. **Найдите настройки:** "Редиректы" или "Переадресация"

4. **Добавьте правило:**
   - Откуда: `*`
   - Куда: `/index.html`
   - Тип: Внутренняя переадресация

---

## ✅ Решение 4: Использовать HashRouter (быстрое решение)

Если ничего не помогает, можно использовать HashRouter вместо BrowserRouter:

### Изменить в `src/App.jsx`:

```javascript
import { HashRouter as Router, Routes, Route } from 'react-router-dom'
```

Тогда URL будут: `/#/blog` вместо `/blog`

---

## 🔍 Проверка:

1. **Убедитесь, что файл `.htaccess` создан** в `public_html`

2. **Проверьте права доступа** на файл (должен быть доступен для чтения)

3. **Проверьте, что включен mod_rewrite** на сервере (обычно включен по умолчанию)

4. **Очистите кеш браузера** (Ctrl+F5)

---

## 📝 Что уже сделано:

- ✅ Создан файл `.htaccess` в папке `dist`
- ✅ Файл будет скопирован при следующей сборке
- ✅ Нужно загрузить его на сервер

---

## 🚀 Быстрое исправление:

1. **Создайте файл `.htaccess`** в `public_html` на сервере

2. **Скопируйте код** из решения 1 выше

3. **Сохраните**

4. **Проверьте** - `/blog` должен работать!

---

**После исправления страница `/blog` будет открываться!** ✅

