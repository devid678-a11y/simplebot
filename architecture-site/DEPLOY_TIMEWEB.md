# 🚀 Деплой на Timeweb + WordPress интеграция

## Шаг 1: Создать Frontend приложение на Timeweb

### Через панель Timeweb Cloud:

1. **Зайдите:** https://timeweb.cloud
2. **Приложения** → **Создать приложение**
3. **Выберите:**
   - **Тип:** Frontend
   - **VCS провайдер:** GitHub (devid678-a11y)
   - **Репозиторий:** `simplebot`
   - **Ветка:** `main`
4. **Настройки:**
   - **Framework:** React
   - **Build команда:** 
     ```
     cd architecture-site && npm install && npm run build
     ```
   - **Index directory:** `architecture-site/dist`
   - **Пресет:** Frontend app (1₽/мес или 99₽/мес)
5. **Название:** `Architecture Bureau`
6. **Нажмите "Создать"**

### После создания:

- Получите URL: `https://architecture-bureau-xxx.timeweb.cloud`
- Сайт автоматически соберется
- При каждом push в Git будет автоматический деплой

---

## Шаг 2: Создать Backend приложение для WordPress

1. **Приложения** → **Создать приложение**
2. **Тип:** Backend
3. **Пресет:** Backend app (ID: 1631 - 250₽/мес)
4. **Название:** `WordPress CMS`
5. **Создать**

### Установка WordPress:

#### Вариант A: Через SSH (рекомендуется)

1. **Подключитесь к приложению через SSH**
2. **Скачайте WordPress:**
   ```bash
   wget https://wordpress.org/latest.tar.gz
   tar -xzf latest.tar.gz
   mv wordpress/* .
   rm -rf wordpress latest.tar.gz
   ```

3. **Создайте базу данных:**
   - Панель Timeweb → Базы данных → Создать MySQL
   - Запомните: имя БД, пользователь, пароль, хост

4. **Настройте wp-config.php:**
   ```bash
   cp wp-config-sample.php wp-config.php
   nano wp-config.php
   ```
   
   Заполните:
   ```php
   define('DB_NAME', 'ваша_база');
   define('DB_USER', 'пользователь');
   define('DB_PASSWORD', 'пароль');
   define('DB_HOST', 'localhost');
   ```

5. **Завершите установку:**
   - Откройте URL приложения в браузере
   - Следуйте инструкциям установки WordPress

#### Вариант B: Через Docker (если доступно)

Создайте `Dockerfile`:
```dockerfile
FROM wordpress:latest
```

---

## Шаг 3: Настроить WordPress REST API

WordPress REST API включен по умолчанию!

**Базовый URL:**
```
https://your-wordpress-app.timeweb.cloud/wp-json/wp/v2/
```

**Полезные эндпоинты:**
- `/wp-json/wp/v2/posts` - посты
- `/wp-json/wp/v2/pages` - страницы
- `/wp-json/wp/v2/media` - медиа
- `/wp-json/wp/v2/categories` - категории

---

## Шаг 4: Интеграция в React

### Создать сервис WordPress API:

Создайте файл `src/services/wordpress.js`:

