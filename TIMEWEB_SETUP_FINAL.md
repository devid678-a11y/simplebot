# Настройка Timeweb API сервера (БЕЗ Firebase)

## Проблема
Timeweb не может найти коммит при клонировании. Решение: создать приложение БЕЗ указания SHA, используя последний коммит из ветки `main`.

## Инструкция по настройке в панели Timeweb

### 1. Создание/Обновление приложения

1. Зайдите на https://console.timeweb.cloud
2. Перейдите в раздел **"Приложения"** или **"Apps"**
3. Если есть старое приложение - удалите его
4. Нажмите **"Создать приложение"**

### 2. Настройки приложения

Заполните форму:

- **Тип**: `backend`
- **Название**: `dvizh-api-server`
- **VCS провайдер**: Выберите ваш GitHub провайдер
- **Репозиторий**: `https://github.com/devid678-a11y/simplebot.git`
- **Ветка**: `main`
- **SHA коммита**: ОСТАВЬТЕ ПУСТЫМ (или используйте последний коммит из ветки)
- **Пресет**: `1631` (или любой другой Backend пресет)
- **Фреймворк**: `express`
- **Build команда**: `npm install` (или оставьте пустым, если npm install выполняется автоматически)
- **Run команда**: `node api/server.js`
- **Port**: `3000` (если есть поле)

### 3. Важно!

- НЕ указывайте SHA коммит - используйте последний коммит из ветки `main`
- Путь к файлу: `api/server.js` (должен существовать в репозитории)
- Run команда: `node api/server.js` (НЕ `node api-server.js`)

### 4. После создания

1. Подождите 2-5 минут пока приложение соберется
2. Проверьте логи приложения - должны быть:
   - `✅ PostgreSQL подключен`
   - `🚀 API сервер запущен на порту 3000`
3. Скопируйте URL приложения (будет что-то вроде `https://xxx.twc1.net`)

### 5. Проверка работы

Откройте в браузере:
- Health: `https://ваш-url.twc1.net/health`
  - Должно вернуть: `{"status":"ok","database":"connected"}`
- События: `https://ваш-url.twc1.net/api/events`
  - Должен вернуть массив событий из PostgreSQL

### 6. Обновление веб-приложения

Веб-приложение уже настроено использовать Timeweb API по умолчанию:
- URL: `https://devid678-a11y-simplebot-a491.twc1.net` (или ваш новый URL)
- Если нужен другой URL, обновите переменную окружения `VITE_API_BASE`

## Структура проекта

```
api/
  server.js    ← API сервер
  package.json ← зависимости (express, pg, cors, dotenv)
```

## Данные базы данных

Учетные данные PostgreSQL встроены в код `api/server.js`:
- Host: `7cedb753215efecb1de53f8c.twc1.net`
- Database: `default_db`
- User: `gen_user`

## Решение проблем

### Ошибка "unable to read tree"
- Не указывайте SHA коммит при создании
- Используйте последний коммит из ветки `main`

### Ошибка "Cannot find module"
- Проверьте Run команду: должно быть `node api/server.js`
- Проверьте путь к файлу в репозитории

### Ошибка подключения к БД
- Проверьте логи приложения
- Учетные данные встроены в код, должны работать автоматически

### SSL ошибка
- Подождите 5-10 минут - Timeweb автоматически настраивает SSL
- Проверьте настройки SSL в панели приложения

