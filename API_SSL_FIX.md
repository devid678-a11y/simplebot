# Исправление проблемы SSL с API сервером

## Проблема
- Ошибка: `ERR_SSL_PROTOCOL_ERROR` при запросе к `https://devid678-a11y-simplebot-a491.twc1.net`
- API сервер недоступен по HTTPS

## Решение

### 1. Проверьте статус приложения в Timeweb

1. Зайдите на https://console.timeweb.cloud
2. Найдите приложение **"dvizh-api-server-v4"**
3. Проверьте:
   - **Статус** - должно быть "Running" или "Запущено"
   - **Логи** - откройте логи и проверьте на ошибки
   - **URL** - должен быть `https://devid678-a11y-simplebot-a491.twc1.net`

### 2. Проверьте логи приложения

В панели Timeweb откройте логи приложения. Должны быть:
- `✅ PostgreSQL подключен`
- `🚀 API сервер запущен на порту 3000`

Если есть ошибки подключения к БД - проверьте учетные данные (они встроены в код).

### 3. SSL сертификат

Timeweb обычно автоматически настраивает SSL сертификат через Let's Encrypt. Это может занять:
- **До 5-10 минут** после первого запуска приложения
- Проверьте в настройках приложения, есть ли SSL сертификат

### 4. Если сервер не запускается

**Проверьте:**
1. **Run команда**: `node api/server.js` (не `node api-server.js`)
2. **Путь к файлу**: должен быть `api/server.js` в корне репозитория
3. **Зависимости**: убедитесь, что `npm install` выполнен успешно
4. **Логи**: проверьте ошибки в логах Timeweb

### 5. Временное решение (если нужно срочно)

Можно использовать другой хостинг для API:
- **Railway** - автоматически настраивает HTTPS
- **Render** - бесплатный тариф с автоматическим HTTPS
- **Vercel** - serverless functions с HTTPS

### 6. После исправления

Когда API сервер заработает:
1. Проверьте: `https://devid678-a11y-simplebot-a491.twc1.net/health`
   - Должно вернуть: `{"status":"ok","database":"connected"}`
2. Проверьте события: `https://devid678-a11y-simplebot-a491.twc1.net/api/events`
   - Должен вернуть массив событий
3. Веб-приложение автоматически подхватит API после обновления страницы

## Проверка статуса

Выполните в консоли браузера (на странице https://dvizh-eacfa.web.app/):

```javascript
fetch('https://devid678-a11y-simplebot-a491.twc1.net/health')
  .then(r => r.json())
  .then(d => console.log('✅ API работает:', d))
  .catch(e => console.error('❌ API не работает:', e.message))
```

Если возвращает ошибку - API сервер не запущен или SSL не настроен.

